{% extends 'base.html.twig' %}

{% block title %}FriendRequest{% endblock %}

{% block body %}
    <h1>FriendRequest</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ friend_request.id }}</td>
            </tr>
            <tr>
                <th>State</th>
                <td>{{ friend_request.state }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_friend_request_index') }}">back to list</a>

    <a href="{{ path('app_friend_request_edit', {'id': friend_request.id}) }}">edit</a>

    {{ include('friend_request/_delete_form.html.twig') }}
{% endblock %}
